picture.hero-content-container
    source(media= '(max-width: ' + breakpoint.small.maxWidth + 'px)' srcset=images.small.url)
    source(media= '(min-width: ' + breakpoint.medium.minWidth + 'px) and (max-width: ' + breakpoint.medium.maxWidth + 'px)'  srcset=images.medium.url)
    source(media= '(min-width: ' + breakpoint.large.minWidth + 'px)'  srcset=images.large.url)
    img(class='hero-content-container__image' srcset=images.small.url alt=images.small.description)
div.hero-content__cta
    div.hero-content__cta-description #{description}
    a(href=cta.url)
        button(class='hero-content__cta-button') #{cta.label}
